# راهنمای کامل: ایجاد محصول و تست پرداخت

این راهنما به صورت گام‌به‌گام شما را در ایجاد محصول جدید و تست جریان پرداخت راهنمایی می‌کند.

---

## 📋 پیش‌نیازها

قبل از شروع، مطمئن شوید که:

- ✅ پروژه به درستی اجرا می‌شود (`npm run dev`)
- ✅ دیتابیس متصل است و migrations اجرا شده‌اند
- ✅ یک حساب کاربری با نقش `SUPER_ADMIN` یا `ADMIN` دارید
- ✅ Merchant ID زرین‌پال در تنظیمات پرداخت وارد شده است
- ✅ زیردامنه `pay.hs6tools.com` در Cloudflare تنظیم شده است (برای تست واقعی)

---

## 🛍️ بخش 1: ایجاد محصول جدید

### مرحله 1: ورود به پنل ادمین

1. **اجرای پروژه:**
   ```bash
   npm run dev
   ```

2. **ورود به پنل ادمین:**
   - به آدرس `http://localhost:3000/fa/admin` بروید
   - با حساب کاربری ادمین وارد شوید

3. **دسترسی به بخش محصولات:**
   - از منوی سمت راست، روی **"محصولات"** (Products) کلیک کنید
   - یا مستقیماً به `http://localhost:3000/fa/admin/products` بروید

### مرحله 2: ایجاد دسته‌بندی (در صورت نیاز)

اگر هنوز دسته‌بندی ندارید:

1. **دسترسی به بخش دسته‌بندی‌ها:**
   - از منوی سمت راست، روی **"دسته‌بندی‌ها"** (Categories) کلیک کنید
   - یا به `http://localhost:3000/fa/admin/categories` بروید

2. **ایجاد دسته‌بندی جدید:**
   - روی دکمه **"افزودن دسته‌بندی"** یا **"+"** کلیک کنید
   - فرم را پر کنید:
     - **نام دسته‌بندی:** مثلاً "ابزار صنعتی"
     - **Slug:** مثلاً "industrial-tools" (به انگلیسی و با خط تیره)
     - **توضیحات:** (اختیاری)
     - **وضعیت:** فعال
   - روی **"ذخیره"** کلیک کنید

### مرحله 3: ایجاد محصول جدید

1. **باز کردن فرم محصول:**
   - در صفحه محصولات (`/fa/admin/products`)
   - روی دکمه **"افزودن محصول"** یا **"+"** کلیک کنید

2. **پر کردن اطلاعات پایه (Basic Info):**
   
   **فیلدهای الزامی:**
   - **SKU:** کد یکتای محصول (مثلاً: `PROD-001`)
   - **نام محصول:** نام فارسی محصول (مثلاً: "پیچ گوشتی برقی")
   - **Slug:** آدرس URL محصول (مثلاً: `electric-screwdriver`)
   - **قیمت:** قیمت به ریال (مثلاً: `500000` برای 50,000 تومان)
   - **دسته‌بندی:** انتخاب دسته‌بندی از لیست
   - **موجودی:** تعداد موجود در انبار (مثلاً: `10`)

   **فیلدهای اختیاری اما توصیه شده:**
   - **قیمت مقایسه:** قیمت قبل از تخفیف (اختیاری)
   - **توضیحات کوتاه:** توضیح مختصر محصول
   - **توضیحات:** توضیحات کامل محصول
   - **تصاویر:** حداقل یک تصویر محصول (الزامی)

3. **آپلود تصاویر:**
   - در بخش **"تصاویر محصول"**
   - روی **"آپلود تصویر"** کلیک کنید
   - حداقل یک تصویر آپلود کنید
   - اولین تصویر به عنوان تصویر اصلی استفاده می‌شود

4. **تنظیمات چندزبانه (Translations):**
   
   برای نمایش در سایت (اختیاری اما توصیه شده):
   - **نام انگلیسی:** English name
   - **نام عربی:** Arabic name
   - **توضیحات انگلیسی:** English description
   - **توضیحات عربی:** Arabic description

5. **تنظیمات پیشرفته (Advanced Settings):**
   
   - **قیمت تمام شده:** قیمت خرید محصول (برای محاسبه سود)
   - **وزن:** وزن محصول به گرم
   - **مواد:** جنس محصول
   - **گارانتی:** مدت گارانتی
   - **برند:** نام برند
   - **حد آستانه موجودی:** حداقل موجودی برای هشدار (پیش‌فرض: 5)

6. **تنظیمات SEO:**
   
   - **Meta Title:** عنوان صفحه برای SEO
   - **Meta Description:** توضیحات برای SEO
   - **Meta Keywords:** کلمات کلیدی

7. **تنظیمات نمایش:**
   
   - **وضعیت:** فعال/غیرفعال
   - **ویژه:** نمایش در صفحه اصلی
   - **ترتیب نمایش:** عدد برای مرتب‌سازی

8. **ذخیره محصول:**
   - روی دکمه **"ذخیره"** یا **"ایجاد"** کلیک کنید
   - منتظر بمانید تا پیام موفقیت نمایش داده شود

### مرحله 4: بررسی محصول

1. **بررسی در لیست محصولات:**
   - محصول جدید باید در لیست محصولات نمایش داده شود

2. **بررسی در فروشگاه:**
   - به `http://localhost:3000/fa/shop` بروید
   - محصول جدید باید در لیست محصولات نمایش داده شود

3. **بررسی صفحه محصول:**
   - روی محصول کلیک کنید
   - به `http://localhost:3000/fa/products/[slug]` بروید
   - اطلاعات محصول باید به درستی نمایش داده شود

---

## 💳 بخش 2: تست جریان پرداخت

### مرحله 1: تنظیمات پرداخت (یک بار)

1. **ورود به تنظیمات پرداخت:**
   - به `http://localhost:3000/fa/admin/settings` بروید
   - روی تب **"تنظیمات پرداخت"** کلیک کنید

2. **وارد کردن اطلاعات زرین‌پال:**
   
   **فیلدهای الزامی:**
   - **شناسه فروشنده (Merchant ID):** `34f387ef-3ba2-41ba-85ee-c86813806726`
   - **کلید API:** ⚠️ **اختیاری** - در API v4 زرین‌پال فقط Merchant ID کافی است. می‌توانید این فیلد را خالی بگذارید یا همان Merchant ID را وارد کنید.
   
   **📍 محل Merchant ID در پنل زرین‌پال:**
   - وارد پنل زرین‌پال شوید: [https://next.zarinpal.com](https://next.zarinpal.com)
   - به بخش **"تنظیمات درگاه"** → **"مشخصات درگاه"** بروید
   - **"کد درگاه پرداخت"** یا **"Merchant ID"** را مشاهده می‌کنید
   - یا در بخش **"Web Services"** / **"API"** می‌توانید Merchant ID را ببینید
   
   **تنظیمات:**
   - **حالت تست (Sandbox):** برای تست فعال کنید ✅
   - **انتقال بانکی:** (اختیاری)
   - **پرداخت در محل:** (اختیاری)

3. **ذخیره تنظیمات:**
   - روی دکمه **"ذخیره تنظیمات"** کلیک کنید

### مرحله 2: ایجاد حساب کاربری تست (در صورت نیاز)

1. **ثبت‌نام کاربر:**
   - به `http://localhost:3000/fa/auth/register` بروید
   - یک حساب کاربری تستی ایجاد کنید
   - یا از حساب موجود استفاده کنید

2. **ورود به حساب:**
   - به `http://localhost:3000/fa/auth/login` بروید
   - وارد حساب کاربری شوید

### مرحله 3: تست جریان خرید کامل

#### گام 1: افزودن محصول به سبد خرید

1. **مشاهده محصول:**
   - به `http://localhost:3000/fa/shop` بروید
   - محصولی که ایجاد کردید را پیدا کنید
   - روی محصول کلیک کنید

2. **افزودن به سبد:**
   - روی دکمه **"افزودن به سبد خرید"** کلیک کنید
   - پیام موفقیت باید نمایش داده شود

3. **بررسی سبد خرید:**
   - روی آیکون سبد خرید در هدر کلیک کنید
   - یا به `http://localhost:3000/fa/cart` بروید
   - محصول باید در سبد خرید باشد

#### گام 2: تکمیل اطلاعات ارسال

1. **رفتن به صفحه تسویه:**
   - در صفحه سبد خرید، روی دکمه **"تسویه حساب"** کلیک کنید
   - یا به `http://localhost:3000/fa/checkout` بروید

2. **مرحله 1: انتخاب آدرس**
   - اگر آدرس ذخیره شده دارید، انتخاب کنید
   - یا آدرس جدید وارد کنید:
     - نام و نام خانوادگی
     - شماره تلفن
     - استان و شهر
     - آدرس کامل
     - کد پستی

3. **مرحله 2: روش ارسال**
   - یکی از روش‌های ارسال را انتخاب کنید:
     - پست
     - تیپاکس
     - پیک سریع

4. **مرحله 3: روش پرداخت**
   - **Zarinpal** را انتخاب کنید (پیش‌فرض)

5. **مرحله 4: بررسی نهایی**
   - خلاصه سفارش را بررسی کنید:
     - محصولات
     - آدرس ارسال
     - روش ارسال
     - مبلغ کل
   - روی دکمه **"ثبت سفارش"** کلیک کنید

#### گام 3: پرداخت در زرین‌پال

1. **هدایت به صفحه پرداخت:**
   - پس از ثبت سفارش، به طور خودکار به صفحه پرداخت زرین‌پال هدایت می‌شوید
   - آدرس باید `pay.hs6tools.com` یا `sandbox.zarinpal.com` باشد

2. **پرداخت تستی:**
   
   **در حالت Sandbox:**
   - از کارت تست زرین‌پال استفاده کنید:
     - **شماره کارت:** `6037-9971-1234-5678`
     - **CVV2:** `1234`
     - **تاریخ انقضا:** هر تاریخ آینده
     - **رمز دوم:** `123456`
   
   - یا از کارت تست دیگری که زرین‌پال ارائه می‌دهد استفاده کنید

3. **تکمیل پرداخت:**
   - اطلاعات کارت را وارد کنید
   - روی **"پرداخت"** کلیک کنید
   - منتظر تأیید پرداخت بمانید

#### گام 4: بازگشت به سایت

1. **هدایت به صفحه موفقیت:**
   - پس از پرداخت موفق، به طور خودکار به صفحه موفقیت هدایت می‌شوید
   - آدرس: `http://localhost:3000/fa/checkout/success?orderNumber=...&refId=...`

2. **بررسی اطلاعات:**
   - شماره سفارش باید نمایش داده شود
   - کد پیگیری پرداخت (refId) باید نمایش داده شود
   - پیام موفقیت باید نمایش داده شود

### مرحله 4: بررسی در پنل ادمین

1. **بررسی سفارش:**
   - به `http://localhost:3000/fa/admin/orders` بروید
   - سفارش جدید باید در لیست باشد

2. **بررسی جزئیات سفارش:**
   - روی سفارش کلیک کنید
   - بررسی کنید:
     - **وضعیت سفارش:** باید `CONFIRMED` باشد
     - **وضعیت پرداخت:** باید `PAID` باشد
     - **کد پیگیری پرداخت:** باید refId نمایش داده شود
     - **تاریخ پرداخت:** باید ثبت شده باشد

3. **بررسی موجودی محصول:**
   - به `http://localhost:3000/fa/admin/products` بروید
   - محصول را باز کنید
   - موجودی باید کاهش یافته باشد (به اندازه تعداد خریداری شده)

---

## 🔍 بررسی و عیب‌یابی

### مشکلات رایج و راه‌حل‌ها

#### مشکل 1: خطا در ایجاد درخواست پرداخت

**علائم:**
- پیام خطا: "خطا در ایجاد درخواست پرداخت"
- هدایت به صفحه پرداخت انجام نمی‌شود

**راه‌حل:**
1. بررسی کنید Merchant ID در تنظیمات پرداخت وارد شده باشد
2. بررسی کنید API Key درست باشد
3. بررسی کنید زیردامنه `pay.hs6tools.com` تنظیم شده باشد
4. لاگ‌های سرور را بررسی کنید (`console.log` در `/api/payment/zarinpal/request`)

#### مشکل 2: خطا در تأیید پرداخت

**علائم:**
- پس از پرداخت، به صفحه خطا هدایت می‌شوید
- پیام: "پرداخت ناموفق بود"

**راه‌حل:**
1. بررسی کنید مبلغ پرداخت با مبلغ سفارش یکسان باشد
2. بررسی کنید Authority درست باشد
3. بررسی کنید Merchant ID درست باشد
4. لاگ‌های سرور را بررسی کنید (`console.log` در `/api/payment/zarinpal/callback`)

#### مشکل 3: سفارش ایجاد می‌شود اما پرداخت انجام نمی‌شود

**علائم:**
- سفارش در دیتابیس ایجاد می‌شود
- اما به صفحه پرداخت هدایت نمی‌شود

**راه‌حل:**
1. بررسی کنید `paymentMethod` در checkout برابر `"ZARINPAL"` باشد
2. بررسی کنید API endpoint `/api/payment/zarinpal/request` درست کار می‌کند
3. بررسی کنید `paymentUrl` از زرین‌پال دریافت می‌شود
4. لاگ‌های مرورگر را بررسی کنید (F12 → Console)

#### مشکل 4: موجودی محصول کاهش نمی‌یابد

**علائم:**
- سفارش ایجاد می‌شود
- اما موجودی محصول تغییر نمی‌کند

**راه‌حل:**
1. بررسی کنید موجودی محصول قبل از خرید کافی باشد
2. بررسی کنید API endpoint `/api/customer/orders` به درستی موجودی را کاهش می‌دهد
3. بررسی کنید transaction در دیتابیس به درستی انجام می‌شود

---

## ✅ چک‌لیست تست

قبل از انتشار به production، این موارد را بررسی کنید:

- [ ] محصول جدید به درستی ایجاد می‌شود
- [ ] محصول در فروشگاه نمایش داده می‌شود
- [ ] افزودن به سبد خرید کار می‌کند
- [ ] صفحه checkout به درستی کار می‌کند
- [ ] درخواست پرداخت به زرین‌پال ارسال می‌شود
- [ ] هدایت به صفحه پرداخت انجام می‌شود
- [ ] پرداخت در حالت Sandbox موفق است
- [ ] بازگشت از زرین‌پال به درستی انجام می‌شود
- [ ] تأیید پرداخت انجام می‌شود
- [ ] وضعیت سفارش به `CONFIRMED` تغییر می‌کند
- [ ] وضعیت پرداخت به `PAID` تغییر می‌کند
- [ ] موجودی محصول کاهش می‌یابد
- [ ] صفحه موفقیت اطلاعات درست را نمایش می‌دهد
- [ ] سفارش در پنل ادمین نمایش داده می‌شود

---

## 📝 یادداشت‌های مهم

1. **حالت Sandbox:**
   - برای تست، همیشه از حالت Sandbox استفاده کنید
   - در حالت Sandbox، پرداخت واقعی انجام نمی‌شود
   - کارت‌های تست زرین‌پال را استفاده کنید

2. **زیردامنه:**
   - برای تست محلی، می‌توانید از `localhost` استفاده کنید
   - برای تست واقعی، زیردامنه `pay.hs6tools.com` باید تنظیم شده باشد

3. **لاگ‌ها:**
   - تمام لاگ‌ها در console مرورگر و terminal سرور نمایش داده می‌شوند
   - برای عیب‌یابی، لاگ‌ها را بررسی کنید

4. **امنیت:**
   - هرگز Merchant ID و API Key را در کد commit نکنید
   - از environment variables استفاده کنید
   - در production، حالت Sandbox را غیرفعال کنید

---

## 🚀 آماده برای Production

قبل از استفاده در production:

1. ✅ Merchant ID و API Key واقعی را وارد کنید
2. ✅ حالت Sandbox را غیرفعال کنید
3. ✅ زیردامنه `pay.hs6tools.com` را در Cloudflare تنظیم کنید
4. ✅ SSL certificate برای زیردامنه فعال باشد
5. ✅ تست کامل جریان پرداخت را انجام دهید
6. ✅ لاگ‌های خطا را بررسی کنید
7. ✅ backup از دیتابیس بگیرید

---

## 📚 منابع بیشتر

- [راهنمای پیاده‌سازی پرداخت زرین‌پال](docs/ZARINPAL_PAYMENT_IMPLEMENTATION.md)
- [راهنمای تنظیم زیردامنه](docs/ZARINPAL_SUBDOMAIN_SETUP.md)
- [مستندات رسمی زرین‌پال](https://www.zarinpal.com/docs/)

---

**نکته:** اگر در هر مرحله مشکلی داشتید، لاگ‌های console و terminal را بررسی کنید و مطمئن شوید که تمام پیش‌نیازها برقرار هستند.

