# راهنمای دریافت API Key از زرین‌پال

## ⚠️ نکته مهم

در **API v4 زرین‌پال** که ما استفاده می‌کنیم، **فقط Merchant ID نیاز است** و API Key جداگانه وجود ندارد!

Merchant ID شما همان چیزی است که برای احراز هویت استفاده می‌شود:
- **Merchant ID:** `34f387ef-3ba2-41ba-85ee-c86813806726`

## 📍 محل Merchant ID در پنل زرین‌پال

### روش 1: از بخش تنظیمات درگاه

1. **ورود به پنل زرین‌پال:**
   - به [https://next.zarinpal.com](https://next.zarinpal.com) بروید
   - وارد حساب کاربری خود شوید

2. **دسترسی به تنظیمات درگاه:**
   - از منوی سمت راست، روی **"تنظیمات درگاه"** کلیک کنید
   - یا مستقیماً به بخش تنظیمات درگاه بروید

3. **مشاهده Merchant ID:**
   - در صفحه تنظیمات درگاه، بخش **"مشخصات درگاه"** را باز کنید
   - **"کد درگاه پرداخت"** یا **"Merchant ID"** را مشاهده می‌کنید
   - این همان کدی است که باید در فیلد "شناسه فروشنده" وارد کنید

### روش 2: از بخش Web Services / API

1. **ورود به پنل زرین‌پال**

2. **دسترسی به بخش Web Services:**
   - از منوی اصلی، بخش **"Web Services"** یا **"API"** را پیدا کنید
   - یا به بخش **"سرویس‌های وب"** بروید

3. **مشاهده اطلاعات:**
   - در این بخش، Merchant ID شما نمایش داده می‌شود
   - ممکن است به صورت **"Merchant ID"** یا **"کد درگاه"** نمایش داده شود

### روش 3: از صفحه اصلی پنل

1. **ورود به پنل زرین‌پال**

2. **مشاهده در داشبورد:**
   - در صفحه اصلی پنل (Dashboard)
   - Merchant ID معمولاً در بالای صفحه یا در بخش اطلاعات حساب نمایش داده می‌شود

## 🔑 درباره API Key

### آیا API Key جداگانه نیاز است؟

**خیر!** در API v4 زرین‌پال:
- ✅ فقط **Merchant ID** برای احراز هویت کافی است
- ❌ API Key جداگانه وجود ندارد
- ✅ Merchant ID همان چیزی است که در تمام درخواست‌های API استفاده می‌شود

### اگر فیلد API Key در فرم خالی است:

**می‌توانید آن را خالی بگذارید** یا همان Merchant ID را در آن هم وارد کنید. کد ما به گونه‌ای نوشته شده که فقط از Merchant ID استفاده می‌کند.

## 📝 تنظیمات در پنل ادمین

در پنل ادمین پروژه شما:

1. **شناسه فروشنده (Merchant ID):**
   ```
   34f387ef-3ba2-41ba-85ee-c86813806726
   ```

2. **کلید API:**
   - می‌توانید **خالی بگذارید** (اختیاری)
   - یا همان Merchant ID را وارد کنید
   - یا اگر در پنل زرین‌پال API Key جداگانه دیدید، آن را وارد کنید

## 🔍 اگر Merchant ID را پیدا نمی‌کنید

### بررسی‌های لازم:

1. **وضعیت حساب:**
   - مطمئن شوید حساب شما **تأیید شده** است
   - حساب‌های تأیید نشده ممکن است دسترسی محدود داشته باشند

2. **نوع حساب:**
   - مطمئن شوید حساب شما از نوع **"فروشنده"** (Merchant) است
   - حساب‌های شخصی ممکن است Merchant ID نداشته باشند

3. **تماس با پشتیبانی:**
   - اگر هنوز Merchant ID را پیدا نمی‌کنید
   - با پشتیبانی زرین‌پال تماس بگیرید
   - ایمیل: support@zarinpal.com
   - تلفن: 021-91070595

## ✅ خلاصه

- **Merchant ID:** `34f387ef-3ba2-41ba-85ee-c86813806726` (این را دارید)
- **API Key:** در API v4 نیاز نیست، می‌توانید خالی بگذارید
- **محل Merchant ID:** تنظیمات درگاه → مشخصات درگاه → کد درگاه پرداخت

## 🧪 تست بدون API Key

می‌توانید تست را بدون وارد کردن API Key انجام دهید:

1. Merchant ID را وارد کنید: `34f387ef-3ba2-41ba-85ee-c86813806726`
2. فیلد API Key را **خالی بگذارید**
3. حالت Sandbox را فعال کنید
4. تست را انجام دهید

اگر خطایی دریافت کردید که مربوط به API Key است، با پشتیبانی زرین‌پال تماس بگیرید.

---

**نکته:** در برخی نسخه‌های قدیمی API زرین‌پال، API Key جداگانه وجود داشت، اما در API v4 که ما استفاده می‌کنیم، فقط Merchant ID کافی است.

